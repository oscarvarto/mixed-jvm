pluginManagement {
    repositories {
        mavenLocal()
        gradlePluginPortal()
        maven {
            url "https://repo.clojars.org"
        }
    }
}

plugins {
    id 'org.gradle.toolchains.foojay-resolver-convention' version '0.9.0'
}

rootProject.name = 'mixed-jvm'
